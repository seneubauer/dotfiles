[bar]
order = [ "top", "side" ]

[bar.top]
concave_edge_corners = true
margin_edge = 0
margin_ends = 0
radius = 0
radius_bottom_left = 6
radius_bottom_right = 6
radius_top_left = 0
radius_top_right = 0
capsule = false
capsule_padding = 9.0
widget_spacing = 12
position = "top"
start = [ "workspaces", "taskbar" ]
center = [ "control-center", "audio_visualizer", "session" ]
{{- if eq .chezmoi.hostname "dwarrowdelf" }}
end = [ "tray", "volume", "brightness", "notifications", "clipboard", "network" ]
{{- else if eq .chezmoi.hostname "ainulindale" }}
end = [ "tray", "battery", "volume", "brightness", "notifications", "clipboard", "network" ]
{{- end }}

[bar.top.actions]
middle = "none"

[bar.side]
concave_edge_corners = true
margin_edge = 0
radius = 0
auto_hide = true
show_on_workspace_switch = true
reserve_space = false
widget_spacing = 12
capsule = false
start = [ "update_templates", "toggle_widget_edit", "toggle_widget_visibility" ]
center = [ "clock" ]
end = [ "open_notes", "gamermode", "keybinds" ]

{{- if eq .chezmoi.hostname "dwarrowdelf" }}

[bar.side.monitor.DP-1]
position = "right"
margin_ends = 300
radius_bottom_left = 6
radius_bottom_right = 0
radius_top_left = 6
radius_top_right = 0

[bar.side.monitor.HDMI-A-1]
position = "left"
margin_ends = 300
radius_bottom_left = 0
radius_bottom_right = 6
radius_top_left = 0
radius_top_right = 6

{{- else if eq .chezmoi.hostname "ainulindale" }}

position = "right"
margin_ends = 300
radius_bottom_left = 6
radius_bottom_right = 0
radius_top_left = 6
radius_top_right = 0

{{- end }}

[widget.workspaces]
style = "minimal"

[widget.control-center]
glyph = "navigation"

[widget.audio_visualizer]
anchor = true
show_when_idle = true
width = 256
bands = 64
capsule = true

[widget.battery]
show_label = false

[widget.volume]
show_label = false

[widget.brightness]
show_label = false

[widget.network]
show_label = false

[widget.update_templates]
type = "custom_button"
glyph = "file-settings"

[widget.update_templates.actions]
left = "exec noctalia msg templates-apply"

[widget.toggle_widget_edit]
type = "custom_button"
glyph = "device-desktop-cog"

[widget.toggle_widget_edit.actions]
left = "exec noctalia msg desktop-widgets-toggle-edit"

[widget.open_notes]
type = "custom_button"
glyph = "notes"

[widget.open_notes.actions]
left = "exec noctalia msg panel-toggle davemhammer/obsidian:manager"

[widget.toggle_widget_visibility]
type = "custom_button"
glyph = "device-desktop"

[widget.toggle_widget_visibility.actions]
left = "exec noctalia msg desktop-widgets-toggle"

[widget.keybinds]
type = "kenn/keybind-cheatsheet:keybinds"

[widget.gamermode]
type = "nomadcxx/gamer-mode:gamermode"

[widget.clock]
format = "{:%H:%M}"
tooltip_format = "{:%Y-%m-%d}"
